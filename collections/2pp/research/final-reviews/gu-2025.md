# Gu 2025 — fresh integrated review

Reviewed by a fresh final-review agent on `codex/2pp-curated-integration`, initially at commit `ca2e5e2834c0aab48cfccc36d1f32e456e2f550f`. This review concerns the integrated scene, not PR 135's original implementation.

**Native optical and visual checks pass. Browser acceptance is blocked.** The useful lesson is intact: an intensity image selects independent metalens axes. The SLM does not create the focusing axes or solve a holographic field.

## Native behavior

I read the integrated scene JSON, evidence note, shared style, relevant mask/metalens/animation implementation and focused tests, then ran `node --test test/2pp-gu-integrated.test.js`: **6/6 pass**. These cover full-aperture admission, field conjugacy including the two modeled windows, region inversion, source gates, finite focus support, stage geometry, sampling independence and JSON round-trip.

Separate native traces confirmed the following:

| Control / condition | Observed result |
| --- | --- |
| Default intensity frame 0 | Six physical metalens channels; 105 spatial/spectral sample arrivals. |
| Uniform frame 1 | Seven physical channels; all 25 spatial × five spectral samples reach resin. |
| Dark frame 2 | No sample or writing arrivals. |
| Frames 3–9 | One channel each, in inverted order M6 through M0. |
| Discrete playback at 0.5 Hz | Frame 0 and six channels persist through 1.999 s; frame 1/seven channels at 2 s; frame 2/dark at 4 s; return to frame 0 at 20 s. No continuously sliding stripe behavior. |
| Source 7 W → 3.5 W | Identical normalized geometric weights and channel positions, as documented. |
| Source 0 W / source disabled | No writing arrivals. |
| Metalens focal length 40 → 60 mm | Seven channels remain; maximum full sample support broadens from 0.276056 to 2.777599 mm. |

The focused test uses all admitted samples, not just chief rays. With the source narrowed to 800 nm, full channel support is below 0.0002 mm. Native small-angle probes give the 75:200 field relay its expected inverted magnification and approximately zero B and C. A 10 mm spacing error introduces residual curvature, so this is a real field relay rather than a visually aligned train. It is not a scanner-to-pupil relay.

The stage's transverse mode moves sample coordinates through fixed optical foci; its Z mode moves the sample plane and broadens the arrivals. JSON save/reload preserves the configured mask, focal-length change, stage mode and trace exactly. Browser file-menu persistence remains a separate open check.

## Native visual review

I generated the scene through the actual SVG exporter and inspected rasterizations at 1500 px and 500 px. These are **native export previews, not browser screenshots**. The 900 × 660 frame contains the source, folded path, explanatory text, SLM frame inset and writing region. The 22-unit body text is approximately 12 px at the narrow raster width. The SLM inset clearly displays the genuine 7×7 intensity frame, current frame number and the amber center-column selection. Its enlarged display does not change the physical SLM aperture.

The source is at upper left and the metalens/resin region at lower right. The on-canvas copy identifies the reported 100 µm / NA 0.8 lens and the independently enlarged 9 mm pitch / 40 mm focal-length surrogate. It explicitly omits the separate observation microscope and says that microscope's objective does not focus the writing beam. There is no extra observation objective in the scene JSON.

Two small findings were sent to the coordinator: the right end of “Metalens row” met the top stage bracket, and the canvas's shortened “Design choices” line omitted the style guide's full interpretation key. **Both are resolved.** I regenerated and inspected the coordinator's corrected scene at 1500 px and 500 px: the relocated metalens label clears the bracket, and the two-line key reads “Design choice = free interpretation; not specified in the paper.” The frame and lower observation note remain clear. The locally corrected scene JSON has SHA-256 `42d379911eba67997c225b44f17a6a81db2b27c08997d19ea47d657515e3c11d`.

## Browser acceptance

On the coordinator's exclusive browser slot, I followed the `control-browser` skill's normal fresh-session bootstrap. It selected **Chrome, CDP, browser ID 1**, and its browser documentation loaded. The first `browser.tabs.list()` call then failed with the exact result:

```text
CDP operation refresh tabs timed out after 20000ms
```

I read the advertised troubleshooting guidance and released the slot. I did not reset the session, switch browser backends, use another browser-control mechanism or retry in a loop. The failure occurred before a Gu page/tab could be reached; it is browser-connection evidence, not evidence of an app error or a site block.

The following required checks remain **blocked and unverified**: final desktop and approximately 1024 px editor layout; toolbar/palette/inspector overflow; actual inspector interactions and playback; file-menu save/reload; console inspection; and browser screenshots. No Gu browser screenshot was captured. Native traces and exports do not substitute for these checks.

## Limits and disposition

The integrated note clearly distinguishes verified source output, reported relay/device properties, modeled window compensation and deliberately enlarged drawing dimensions. The 20 nm ray spread is geometric chromatic support, not a physical PSF or voxel. Positive watts do not calibrate dose; the shared intensity frame is a representative column through a 2D display; there is no phase-to-intensity Jones calibration, electromagnetic metalens field, polymerization or throughput model.

**Native acceptance; browser acceptance blocked.** The two visual/copy findings are resolved. Real-browser acceptance remains an explicit open delivery gate. No scene or shared-component files were edited by this reviewer.
